# patch_nfs_details.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nfs-synology-provisioner
  name: nfs-synology-provisioner
spec:
  template:
    spec:
      containers:
        - name: nfs-synology-provisioner
          env:
            - name: NFS_SERVER
              value: 172.20.20.16
            - name: NFS_PATH
              value: /var/k8s-data
            - name: PROVISIONER_NAME
              value: simplefxn.com/nfs
      volumes:
        - name: nfs-client-root
          nfs:
            server: 172.20.20.16
            path: /var/k8s-data