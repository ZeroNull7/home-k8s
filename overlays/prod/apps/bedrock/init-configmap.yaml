apiVersion: v1
kind: ConfigMap
metadata:
  name: bedrock-init-script
  namespace: apps
data:
  test.sh: |
    #!/bin/bash
    set -eo pipefail

    : "${TMP_DIR:=/tmp}"

    if [[ ${DEBUG^^} = TRUE ]]; then
      set -x
      curlArgs=(-v)
      echo "DEBUG: running as $(id -a) with $(ls -ld /data)"
      echo "       current directory is $(pwd)"
    fi