apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: trilium
  name: trilium
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
