apiVersion: v1
kind: ServiceAccount
metadata:
  name: nfs-local-provisioner
  # replace with namespace where provisioner is deployed
  namespace: infra
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: nfs-local-provisioner-runner
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "update", "patch"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: run-nfs-local-provisioner
subjects:
  - kind: ServiceAccount
    name: nfs-local-provisioner
    # replace with namespace where provisioner is deployed
    namespace: infra
roleRef:
  kind: ClusterRole
  name: nfs-local-provisioner-runner
  apiGroup: rbac.authorization.k8s.io
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: leader-locking-nfs-local-provisioner
  # replace with namespace where provisioner is deployed
  namespace: infra
rules:
  - apiGroups: [""]
    resources: ["endpoints"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: leader-locking-nfs-local-provisioner
  # replace with namespace where provisioner is deployed
  namespace: infra
subjects:
  - kind: ServiceAccount
    name: nfs-local-provisioner
    # replace with namespace where provisioner is deployed
    namespace: infra
roleRef:
  kind: Role
  name: leader-locking-nfs-local-provisioner
  apiGroup: rbac.authorization.k8s.io
