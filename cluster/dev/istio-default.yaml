---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: istio-default
  namespace: flux-system
spec:
  dependsOn:
    - name: istio-operator
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m0s
  path: ./infra/istio/default
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system