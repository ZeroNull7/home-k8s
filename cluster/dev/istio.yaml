---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: istio
  namespace: istio-system
spec:
  dependsOn:
    - name: istio-operator
      namespace: istio-operator
    - name: metallb
      namespace: metallb-system
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m0s
  path: ./istio-system/default
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system